# xxsj_web16

> [!NOTE]
>
> `tz.php` 雅黑探针修改版来自于：[`Luminous`](https://luotianyi.vc/1830.html)
>
> `index.html` 以及配套 `js` 来自于：[`Hsiao-Feng/Periodic-Table`](https://github.com/Hsiao-Feng/Periodic-Table)

1. 访问 `URL/tz.php` 来到 `雅黑探针` 页面
2. 访问 `URL/tz.php?act=phpinfo` 来到 `phpinfeo` 页面
3. 使用 <kbd>CTRL</kbd>+<kbd>F</kbd> 搜索 `flag` 关键字找到 `flag`